package ${packages};

import org.nutz.web.WebLauncher;

public class ${mainName}Launcher extends WebLauncher {

    public static void main(String[] args) {
        WebLauncher.start(args);
    }
    
}