package ${packages};

import org.nutz.mvc.NutConfig;
import org.nutz.mvc.Setup;

public class ${mainName}Setup implements Setup {

    @Override
    public void init(NutConfig nc) {}

    @Override
    public void destroy(NutConfig nc) {}

}
